var str="the rain in the spain mainly in the plain";
//slice, substring, substr
//slice-start,end index, negative values for backtracing
//substring- start,end index, doesnt accept negative values
//substr - start, length
var newstr=str.toUpperCase();
console.log(newstr.charAt(2));